[project]
name = "recian-api"
description = "Recian Python API"
readme = "README.md"
authors = [
    { name = "Shane Spencer", email = "305301+whardier@users.noreply.github.com" }
]
requires-python = ">=3.10"
dependencies = []

[project.scripts]
recian-api = "recian_api:main"

[build-system]
build-backend = "hatchling.build"
requires = ["hatch-vcs", "hatchling"]

[tool.hatch.version]
source = "vcs"
